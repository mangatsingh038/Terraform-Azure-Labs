name: Self Hosted Runner Test

on:
  push:
    branches:
      - feature/new-code

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

     - name: Test Runner
  run: echo "Hello from my self-hosted runner - TEST 2!"

      - name: Check Windows Machine
        run: |
          hostname
          whoami
          systeminfo | findstr /B /C:"OS Name" /C:"OS Version"
